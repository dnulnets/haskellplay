module Main where

-- import Control.Monad.Error
import Network.SSH.Client.SimpleSSH

main :: IO ()
main = do
  val <- runSimpleSSH $
    withSessionPassword "127.0.0.1" 22 "/home/tomas/.ssh/known_hosts" "tomas" "#C0lgate!#" $ \handle -> execCommand handle "ls"
  putStrLn $ show val
  
  

